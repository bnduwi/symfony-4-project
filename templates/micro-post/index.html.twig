{% extends 'base.html.twig' %}

{% block body %}
    <h1 >Recent updates</h1>
        
        {% for post in posts %}
            <div class="media text-muted pt-3">
                
                <p > {{ post.text }}</p>
                
            </div>
        {% endfor %}    
    
{% endblock %}

